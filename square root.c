#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    double a, squareroot;
    printf("");
    scanf("%lf",&a);

    squareroot = sqrt(a);
    printf("%.1lf",squareroot);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}
